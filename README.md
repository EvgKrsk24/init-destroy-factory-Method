# init-destroy-factory-Method
The project shows 
init and destroy methods in the bean life cycle. The configuration of the init and destroy methods is done in applicationContext.xml. To show, run the downloaded project.
